#include <iostream>

class Solution {
    public:
        void Abbreviate(std::string target);
};

void Solution::Abbreviate(std::string target) {
    if(target.size() <= 10){
        std::cout << target << std::endl;
    }
    else{
        
    }
}